# Patterns

## Findings

[2026-06-01] [Code-Review] [CODE-REVIEW] berlinstartup.js: res.text() outside try/catch in fetchDetail — body-read errors bypass prod fail-closed handler | ADVISORY
[2026-06-01] [Code-Review] [CODE-REVIEW] Task D6a — CLEAN PASS (0 blocking, 1 advisory)
[2026-06-05] [Workflow] [DOCS] Box-drawing glyphs (│└┴) don't survive nested heredocs in MINGW→WSL pipe — codepoint drift breaks exact-byte match. Rule: author docs in portable ASCII; reserve Unicode for editor-typed content, never piped. | RESOLVED
[2026-06-05] [Workflow] [VERIFICATION] Shipped sync-project-assets apply path without executing it — set -u + empty array (TO_READD) crashed on first real apply. Failed safe via set -euo pipefail. W5 applied to own code: classifier was behavior-verified, writer path was only prose-verified. Rule: a script's destructive path must be run, not reasoned about, before it's trusted. | RESOLVED
[2026-06-05] [Workflow] [BOOTSTRAP] Suspected bootstrap-project.sh double-write of coordinator.md to .asset-manifest (appears twice). Behavior test 2026-06-06 (fresh scratch bootstrap) records each asset exactly once — coordinator single line, uniq -d empty; deployed jobs-radar manifest was healed by the 06-05 re-sync. Writer is clean, not reproducible; prose-predicted then behavior-confirmed. | RESOLVED
[2026-06-06] [Workflow] [MANIFEST] .asset-manifest is 2-column <path>\t<sha256>; engineering-standards.md sources from docs/standards/ not .claude/, so sync-project-assets.sh hardcodes the remap in repo_source_for() (its own TODO flags this). Add a 3rd source-path column so the special-case dies and unmapped sources fail safe. | RESOLVED (369d306, 72315d7)
[2026-06-05] [Workflow] [RETRO-ACCURACY] Retro item #2 ("delete stale ~/.claude, safe") was wrong — dir held live memory (19 files incl Sovary project memory), active logs, sessions, backups, a credentials reference. Only agents/commands/skills (May 13) were fossil. Rule: "safe to delete" from a prior session is an assertion, not evidence; ls before rm, always. | RESOLVED
[2026-06-05] [Workflow] [GIT-IDENTITY] WSL2 git and MINGW git have separate configs — commits authored in the desktop app used a stale noreply email while WSL2 config showed the correct one. The identity you see in one runtime is not what commits in the other. Fixed MINGW global to thomas@thomasloosch.dev. | RESOLVED
[2026-06-05] [Workflow] [W4-HOLED-GATE] coordinator W4 gate (commit 0213d92) shipped non-functional — it grepped the compliance log for code-review PASS lines, but code-review wrote those lines via unset $CLAUDE_LOGS_DIR to a broken path. Reader and writer pointed at different files; gate would falsely report "review didn't run." Caught by tracing reader→writer, NOT by Stage 3 (which would have mislabeled it "gate logic broken"). Lesson: a gate is only as good as the plumbing between its check and the artifact — verify the artifact path, not just the check logic. | RESOLVED (18d884c, b06bf5d)
[2026-06-05] [Workflow] [CLAUDE-VARS-UNSET] All $CLAUDE_* env vars (LOGS_DIR, MEMORY_DIR, HOME) are assumed-set across agents/commands but UNSET in the MINGW desktop runtime — every reference is a silent-failure path. LOGS_DIR broke W4 visibly; MEMORY_DIR (~20 sites) silently degrades agent lessons/memory reads with no crash symptom (the dangerous kind). LOGS_DIR fixed repo-wide; MEMORY_DIR + HOME are a scoped workstream requiring a resolution check first (WSL2 ~/.claude/memory vs Windows $HOME/.claude/memory diverge — blind substitution would point agents at the wrong tree). | RESOLVED (1aad210, d4e0106, 627a632)
[2026-06-05] [Workflow] [STAGE3-PREREQ] The $CLAUDE_MEMORY_DIR fix is a Stage 3 PREREQUISITE, not a follow-up: if agents can't read lessons/patterns (broken memory path), the W1-W5 prompt fixes get validated against agents running lessons-blind, missing half their context. Memory-dir fix must land before Stage 3 validation is meaningful. | RESOLVED (path fix + content-existence b43b1b7)
