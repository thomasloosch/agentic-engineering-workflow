# Project Conventions — {{PROJECT_NAME}}

@.claude/engineering-standards.md

This file is read by Claude Code at the start of every session. It overrides or extends the global agentic-engineering-workflow defaults. The `@` line imports the workflow's baseline engineering standards.

---

## Project quick context

- **Language:** [Primary language, e.g., TypeScript / Python / Go]
- **Framework:** [If applicable — React, Vite, FastAPI, etc.]
- **Deployment target:** [Hetzner / Vercel / serverless / etc.]
- **Production URL:** [if applicable]
- **Database:** [if applicable]

## Engineering standards overrides

[Use this section to opt out of any default standard. Leave empty if all defaults apply.]

## Project-specific patterns

[Patterns specific to this codebase that agents should know. Example: "All API responses use the {success, data, error} envelope shape."]

## Standing rules (graduated from lessons)

[Promoted lessons specific to this project. New rules added here only after 3+ recurrences in lessons.md.]

## Path-scoped rules

For rules that apply only to specific file types or directories (e.g., "rules for backend routes", "rules for migration files"), use `.claude/rules/<topic>.md` with YAML `paths:` frontmatter rather than adding entries here. See `.claude/rules/README.md` for syntax and gotchas.

Keep this top-level CLAUDE.md for project-wide rules only. Anything narrower belongs in `.claude/rules/`.

## Domain glossary

[Terms specific to this domain that agents should understand correctly. Example: "Frist = legal deadline; do not translate as 'reminder'."]

## Bilingual policy

[If applicable. Example: "All user-facing strings exist in locales/de/translation.json AND locales/en/translation.json at commit time."]
